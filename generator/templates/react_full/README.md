# {{ project_name }} (React static)

App React gerado automaticamente (static placeholder).

Como executar (exemplo):

npm install -g serve
# depois (opcional) gerar build e servir a pasta build como estático
serve -s build
